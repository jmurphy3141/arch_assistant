# API base URL — must be a relative path for same-origin LB routing.
# Default /api maps to /api/* which the OCI LB routes to FastAPI on port 8000.
# Do NOT set this to an absolute URL in production deployments.
VITE_API_BASE_URL=/api
