# OCI BOM Generator (External Source Snapshot)

Reference copy of external BOM service README content provided in handoff.

Highlights captured for v1.7 planning:

1. Conversational OCI advisor + BOM generator workflow.
2. Oracle price list grounding from `apexapps.oracle.com`.
3. On-demand caches for pricing, compute shapes, and services catalog.
4. Response mode contract: advisory (`normal`), clarifying (`question`), finalized BOM (`final`).
5. Editable BOM review and XLSX export behavior.
6. Standalone OIDC/session model in source app (to be adapted to existing platform auth).

Use this document as requirements context; implement in existing app style/UI.
