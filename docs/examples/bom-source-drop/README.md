# BOM Source Drop (Reference)

These files are preserved as reference artifacts provided for v1.7 integration planning.

They are examples only and are **not** the target runtime architecture for this repo.

## Files

- `main.py.example` — imported standalone FastAPI BOM backend snapshot (reference excerpt)
- `index.html.example` — imported standalone BOM UI snapshot (reference excerpt)
- `admin.html.example` — imported standalone admin UI snapshot
- `README.external.example.md` — imported standalone project README snapshot

## Integration Decision

- Keep backend BOM logic and API behavior.
- Do **not** adopt standalone UI pages.
- Implement BOM UX in existing app UI.
