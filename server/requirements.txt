fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-multipart>=0.0.9
openpyxl>=3.1.0
pyyaml>=6.0
anyio>=4.0.0
httpx>=0.27.0

# OCI SDK with ADK (Agent Development Kit) — required for production OCI auth
# Optional for local dev/testing (server degrades gracefully when unavailable)
oci[adk]==2.165.1
